import { ClassroomCreateView } from 'src/sections/classroom/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Questify | Create a new classroom',
};

export default function ClassroomCreatePage() {
  return <ClassroomCreateView />;
}
